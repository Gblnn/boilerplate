import {
  getAllSuppliers,
  addSupplier,
  createRestockRecord,
  getRestockRecords,
  getAllProducts,
  deleteSupplier,
} from "@/services/firebase/pos";
import { doc, updateDoc } from "firebase/firestore";
// ... existing code ...
